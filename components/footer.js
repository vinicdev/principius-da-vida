import styled from "styled-components"

const Container = styled.section`
    padding: 40px;
`;

export default function Footer() {
    return (
        <Container>
            Footer
        </Container>
    )
}